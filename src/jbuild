(jbuild_version 1)

(executable
 ((name        aws_gen)
  (flags (:standard w))         ; Temp disable of unused variable warnings.
  (libraries ("yojson" "cmdliner" "ppx_tools" "ocaml-migrate-parsetree"))))

(rule (with-stdout-to structures.ml
       (progn
        (echo "[@@@warning \"-a\"]\nmodule rec HACK : sig\n")
        (cat structures.mli)
        (echo "\nend = HACK\ninclude HACK\n"))))
